<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">

    <title>HelpUhelp</title>

    <link rel="icon" type="image/png" href="<% asset_path 'favicon.png'%>" >

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="referrer" content="no-referrer">

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar">
      <div class="navbar-container">
        <% if current_user %>
        <a class="navbar-brand" href="/tasks">
        <% else %>
        <a class="navbar-brand" href="/login">
        <% end %>
          <%= image_tag('help-U-help.png', size: "168x50", alt: "Aufgaben", class: 'd-inline-block') %>
        </a>
          <ul class="navbar-ul">

            <% if current_user %>
              <% if current_user.role.title == "admin" %>
                <li class="navbar-li"><%= link_to "Nutzer", users_path %></li>
              <% end %>
            <li class="navbar-li"><%= link_to "Aufgaben", tasks_path %></li>
            <li class="navbar-li">
              <%= link_to "Mein Profil", user_path(current_user.id) %>
            </li>
            <li class="navbar-li"><%= link_to "ausloggen", logout_path %></li>
            <% end %>

          </ul>
        </div>
    </nav>
    <%= yield %>
  </body>
</html>
