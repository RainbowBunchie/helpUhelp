<%= form_with(model: status_task_user, local: true) do |form| %>
  <% if status_task_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_task_user.errors.count, "error") %> prohibited this status_task_user from being saved:</h2>

      <ul>
      <% status_task_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :status_task_user_user_id %>
  </div>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.text_field :task_id, id: :status_task_user_task_id %>
  </div>

  <div class="field">
    <%= form.label :status_id %>
    <%= form.text_field :status_id, id: :status_task_user_status_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
